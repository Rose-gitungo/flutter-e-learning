class UserProfile {
  final String username;
  final String email;

  UserProfile(this.username, this.email);
}

// Creating an instance of UserProfile and passing in the values
final userProfile = UserProfile('john_doe', 'john.doe@example.com');
